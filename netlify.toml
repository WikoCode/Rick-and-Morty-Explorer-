# Netlify deployment configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side routes (e.g. /characters, /character/1)
# resolve to index.html instead of a 404 on direct visit / refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
